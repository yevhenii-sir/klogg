diff --git a/karchive/src/kar.cpp b/karchive/src/kar.cpp
index 4e3d3c1..1234567 100644
--- a/karchive/src/kar.cpp
+++ b/karchive/src/kar.cpp
@@ -102,7 +102,7 @@ bool KAr::openArchive(QIODevice::OpenMode mode)
         return true;
     }
     if (mode != QIODevice::ReadOnly && mode != QIODevice::ReadWrite) {
-        setErrorString(tr("Unsupported mode %1").arg(mode));
+        setErrorString(tr("Unsupported mode %1").arg((int)mode));
         return false;
     }
 
diff --git a/karchive/src/karchive.cpp b/karchive/src/karchive.cpp
index 5e6f7a8..8901234 100644
--- a/karchive/src/karchive.cpp
+++ b/karchive/src/karchive.cpp
@@ -166,7 +166,7 @@ bool KArchive::open(QIODevice::OpenMode mode)
     }
 
     if (!d->dev->isOpen() && !d->dev->open(mode)) {
-        setErrorString(tr("Could not set device mode to %1").arg(mode));
+        setErrorString(tr("Could not set device mode to %1").arg((int)mode));
         return false;
     }
 
@@ -214,7 +214,7 @@ bool KArchive::createDevice(QIODevice::OpenMode mode)
         }
         break; // continued below
     default:
-        setErrorString(tr("Unsupported mode %1").arg(d->mode));
+        setErrorString(tr("Unsupported mode %1").arg((int)d->mode));
         return false;
     }
     return true;
